# 브리핑 분석기 개발일지

## 2026-05-26

### 타임스탬프 오차 문제 해결
**증상**
- 실제 발화 시간과 STT 타임스탬프가 최대 10초 가량 밀리는 현상
- 예) 실제 00:00:29 발화 → STT 결과 00:00:19로 기록

**원인**
- merge_segments() 함수가 여러 세그먼트를 첫 번째 세그먼트의
  start 시간으로 묶어버림
- MERGE_MAX_SEC(10초) 범위 내 발화들이 전부 첫 타임스탬프로 기록됨

**해결**
- merge_segments()를 passthrough로 변경
- 세그먼트 병합 없이 개별 세그먼트를 그대로 Claude에 전달
- 타임스탬프 오차 0으로 개선

**트레이드오프**
- 토큰 사용량 약 30~40% 증가 (2시간 영상 기준 $0.05~0.1 수준)
- 타임스탬프 정확도 확보가 더 중요하다고 판단하여 적용

**현재 남은 오차**
- vad_filter 기반 오차 2~3초는 허용 범위로 간주
- 인게임 사운드 제거(2단계) 이후 재측정 예정

---

### faster-whisper large-v3 도입
**변경 전**: openai-whisper medium
**변경 후**: faster-whisper large-v3

**이유**
- 동일 정확도에서 4배 빠른 속도
- large-v3은 medium보다 정확도 높고 한국어 인식률 우수
- RTX 3080 VRAM 10GB에서 정상 작동 확인

**결과**
- 2시간 영상 기준 25분 → 6~10분으로 단축
- CUDA 가속 정상 작동 (GPU 사용률 98% 확인)

---

### STT 캐시 기능 추가
- 동일 URL 재분석 시 STT 건너뛰고 캐시 로드
- 캐시 파일명에 모델명 포함 ({url_md5}_{model}.json)
- 모델별 캐시 분리 저장으로 충돌 방지

---

### 청크당 세그먼트 수 제한 추가
**문제**
- MAX_CHARS=5000으로 입력 크기만 제한
- medium 모델은 텍스트가 짧아 132개가 1청크에 몰림
- Claude 출력이 8192 토큰 초과 → JSON 잘림 → 브리핑 0개

**해결**
- 청크당 최대 세그먼트 수 50개로 제한 추가
- 입력 크기(MAX_CHARS) + 세그먼트 수(MAX_SEGS) 이중 제한

---

### Whisper 환각 필터링 추가
**문제**
- 동일한 텍스트가 연속으로 반복 인식되는 현상
- 예) "공략 잡았어" x4, "아앜" x50
- 원인 1: 무음 간격 1.0초로 짧은 발화가 잘게 쪼개짐
- 원인 2: Whisper가 무음/배경음 구간에서 직전 발화 복붙

**해결**
- MERGE_GAP_SEC 1.0 → 1.5초로 변경
- 동일 텍스트 2회 이상 연속 시 첫 번째만 유지
- 영웅 이름 / 포커싱 키워드 포함 시 제거 제외
  (포커싱 브리핑은 의도적 반복이므로)

---

### Claude 출력 토큰 최적화
**문제**
- Claude가 입력 세그먼트 텍스트를 출력에 그대로 복사해서 반환
- 출력 토큰이 입력과 거의 동일 → 비용 비효율

**해결**
- Claude 반환 형식을 {"time", "tag"} 만으로 축소 (text 제거)
- extractor.py에서 API 응답 후 time 기준 역매핑으로 text 복원
- max_tokens 8192 → 4096으로 감소 (출력이 절반 이하로 줄어 안전)
- 동일 초 충돌 시 첫 번째 세그먼트 우선 매핑

**예상 효과**
- 출력 토큰 70~80% 감소
- 비용 $2.97 → $0.60 수준 (2시간 영상 기준)

---

### Whisper initial_prompt 전면 업데이트
**내용**: 전체 영웅(탱커/딜러/서포터) 이름, 스킬명, 별칭 309개 용어 추가
**기대 효과**: 영웅 이름 및 스킬 오인식률 대폭 감소

---

### initial_prompt 전면 통합 업데이트
- 영웅별 스킬명 세분화 (한글 정식명 + 실전 줄임말 통합)
- 선수 닉네임 한글/영어 통합 (Team Falcons 2026 로스터 기반)
- 궁극기 별칭에 슬램 추가
